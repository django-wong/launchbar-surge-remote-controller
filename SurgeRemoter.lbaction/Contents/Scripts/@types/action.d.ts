declare namespace Action {
	const path: string;

	const scriptType: string;

	const version: string;

	const bundleIdentifier: string;

	const cachePath: stirng;

	const supportPath: string;

	const debugLogEnabled: boolean;

	const preferences: {
		[key: string]: any
	}
}